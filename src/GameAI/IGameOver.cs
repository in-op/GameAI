namespace GameAI
{
    public interface IGameOver
    {
        /// <summary>
        /// Returns true if the game is over, false otherwise.
        /// </summary>
        /// <returns></returns>
        bool IsGameOver();
    }
}
